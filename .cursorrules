强制安全底线：在执行重构或代码清理时，必须100%保留现有的所有代码注释（包括 JSDoc、业务逻辑说明、待办事项等），绝对不允许将注释视为“冗余”而删除。

架构规范：所有前后端 API 对接必须通过环境变量（.env）读取 baseURL，严禁在组件中硬编码 URL 接口。

UI与交互规范：深色/浅色模式切换必须使用 CSS 变量，严禁出现页面刷新时的样式闪烁（FOUC）。

确保所有数据都有实时链接，不会出现数据无法保存加载的情况；所有人机交互的弹窗提示不会出现多次重复提示或者两者的冲突

确保不会出现留白过大，布局混乱的情况，界面清晰，易于使用。

确保所有手机端和PC端都能正常使用，不会出现兼容性问题出现手机端访问有表格或者数据拥挤出界等情况。